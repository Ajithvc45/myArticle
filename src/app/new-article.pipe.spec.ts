import { NewArticlePipe } from './new-article.pipe';

describe('NewArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new NewArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
